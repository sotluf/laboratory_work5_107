#include <iostream>
#include "windows.h"
int main()
{
SetConsoleCP(1251);
SetConsoleOutputCP(1251);

const int n = 10;
int A[n];
int b = 100;
int a = -100;

std::cout << "Привіт, Олександро! \n";

cout << "Масив:";
srand(time(NULL));
for (int i = 0; i < n; i++)
{
   A[i] = rand() % (b - a + 1) + a;
   cout << " " << A[i];
}
}
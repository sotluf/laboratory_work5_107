#include <iostream>
#include "windows.h"
int main()
{
SetConsoleCP(1251);
SetConsoleOutputCP(1251);
std::cout << "Привіт, Олександро! \n";

const int n = 10;
int A[n];
int b = 100;
int a = -100;

cout << "Масив:";
srand(time(NULL));
    for (int i = 0; i < n; i++)
{
   A[i] = rand() % (b - a + 1) + a;
   cout << " " << A[i];
}

cout << endl << "Масив з парними індексами:";
for (int i = 0; i < n; i++)
  if (i % 2 == 0) cout << " " << A[i];

cout << endl << "Індекс останнього негативного елемента:";
for (int i = n-1; i >= n; --i)
    if (A[i] < 0) {
        cout << " " << i;
        break;
}

}
